interface:
  display_name: "Herald Release"
  short_description: "Publish Herald beta tags and docs updates"
  default_prompt: "Use $release to review Herald changes since the last tag, choose the beta version bump, update What's New docs, tag, push, monitor CI/CD, and write release notes."
